/**
 * decision's own copy of every domain's package-private Stage 1-3 output (evidence/inflection/
 * sequences) plus Stage 4/5's own package-private output (convergence/lifecycle) - the same
 * boundary {@code discovery.convergence}'s own {@code AbstractSequenceReader} already crossed one
 * layer down for Stage 3 alone, extended here to Stage 1/2 as well, one layer up. Powers {@code
 * api.opportunities}, the read-only API behind the Opportunities Dashboard and Opportunity Detail
 * page.
 */
package com.alphagraph.decision.opportunity;
