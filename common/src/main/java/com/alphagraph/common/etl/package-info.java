/**
 * Generic ETL pipeline contracts (Collector, Parser, Validator, Normalizer, Loader) that every
 * data source across every module ingests through — see docs/002_Engine_Architecture.md §2.
 */
package com.alphagraph.common.etl;
