spring:
  application:
    name: alphagraph
  profiles:
    default: local
  flyway:
    enabled: false # migrations run per-module via FlywayMultiSchemaConfig instead

management:
  endpoints:
    web:
      exposure:
        include: health
